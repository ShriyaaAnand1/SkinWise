import React from "react";
import ReactDOM from "react-dom/client";
import SkinWiseSidebar from "./app/components/SkinWiseSidebar";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SkinWiseSidebar />
  </React.StrictMode>
);